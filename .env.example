# Google OAuth Web client ID (not a client secret).
GOOGLE_CLIENT_ID=
# Add Neon via Vercel Storage / Marketplace and connect it to this project.
DATABASE_URL=
# Optional fixed production origin, e.g. https://your-project.vercel.app
# Leave unset until the final domain is known. If set, it must match the URL used.
APP_URL=
